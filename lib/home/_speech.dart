import 'package:flutter/material.dart';

class HomeSpeechScreen extends StatelessWidget {
  const HomeSpeechScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Coming soon!'),
      ),
    );
  }
}
